name: Deploy to Production

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
          # Navigate to broadcast-app
          cd broadcast-app

          # Install dependencies
          npm install

          # Build Next.js app
          npm run build

          # Reload PM2 process
          pm2 reload ecosystem.config.js
